import React, { useState } from 'react';

const useSubmit = ({ packs, price, quitDate, setErrors }) => {
	let errors = {};

	const [submitting, setSubmitting] = useState();
};

export default useSubmit;
